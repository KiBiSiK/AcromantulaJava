package net.cydhra.acromantula.java.mapping.visitors

import net.cydhra.acromantula.workspace.filesystem.FileEntity

/**
 * @param file the database file handle
 * @param ownerIdentity the identity of the symbol that owns the annotations being visited
 */
class MapperAnnotationVisitor(private val file: FileEntity, private val ownerIdentity: String) {

    suspend fun visit(

    ) {

    }
}